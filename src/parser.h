#ifndef PARSER_H_
#define PARSER_H_

#include <fstream>
#include <vector>
#include <string>

std::vector<string> parseInstructions(string& filename) {
	std::vector<string> instructions;

	// TODO: parse file

	return instructions;
}

#endif /* PARSER_H_ */